# Minimal header and navigation update

## What will change
- Resize the Labmed logo responsively so it begins near the left edge and extends naturally toward the three-line menu without crowding it.
- Reduce excess header height and spacing while preserving a comfortable tap area for the menu.
- Simplify the mobile menu into a clean, full-width list with clear active-page styling and the two contact numbers grouped neatly below it.
- Refine desktop navigation spacing so the logo, links, and call button remain balanced at common screen widths.
- Keep the existing logo artwork, pages, colors, contact details, and page content unchanged.

## Validation
- Check the header and open menu at phone, tablet, and desktop sizes.
- Confirm the logo never overlaps the menu, navigation labels fit, page links work, and the header remains readable over both page imagery and white content.
- Confirm the site still builds without errors.

## Technical details
- Update the shared header and logo presentation only, using the existing design tokens and responsive breakpoints.
- Preserve the current fixed-header scroll behavior and accessible menu labels.
